/** Added alongside SmokingStatus as a practical, standard intake-form addition (see NutritionAssessment). */
export enum AlcoholUse {
  NONE = "none",
  OCCASIONAL = "occasional",
  REGULAR = "regular",
}
