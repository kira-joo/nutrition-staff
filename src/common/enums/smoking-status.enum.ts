/**
 * Not in the original assessment field list — added as a practical
 * refinement: smoking is a standard question on any real nutrition/medical
 * intake form and materially affects metabolic/clinical recommendations.
 */
export enum SmokingStatus {
  NEVER = "never",
  FORMER = "former",
  CURRENT = "current",
}
