/** Matches nutrition-client's public-facing 5-tier model and multipliers exactly, for business-wide consistency (see the Calculation Workspace). */
export enum ActivityLevel {
  SEDENTARY = "sedentary",
  LIGHT = "light",
  MODERATE = "moderate",
  ACTIVE = "active",
  VERY_ACTIVE = "very_active",
}
