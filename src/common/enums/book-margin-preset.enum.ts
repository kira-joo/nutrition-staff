/** Good defaults over complicated print configuration — three named presets, not raw millimeter inputs exposed to the doctor. */
export enum BookMarginPreset {
  COMPACT = "compact",
  STANDARD = "standard",
  GENEROUS = "generous",
}
