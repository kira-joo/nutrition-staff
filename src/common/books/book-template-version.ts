/**
 * V1 has exactly one visual template. This constant still exists so a
 * future visual change ships as `dr-omnia-book-v2` — additive only, never
 * replacing this string — and so historical Editions can record which
 * version rendered them without ambiguity.
 */
export const CURRENT_BOOK_TEMPLATE_VERSION = "dr-omnia-book-v1";
